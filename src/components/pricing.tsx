"use client";
import Link from "next/link"; import { Check,ArrowRight } from "lucide-react"; import { useState } from "react"; import { plans } from "@/lib/mock"; import { SiteHeader } from "./site-header";
export function Pricing(){const [yearly,setYearly]=useState(true);return <><SiteHeader/><main className="pricing-page"><span className="eyebrow">SIMPLE, TRANSPARENT PRICING</span><h1>Start free. Grow your way.</h1><p>Every plan includes a beautiful, fast profile. Upgrade when you’re ready for more.</p><div className="billing-toggle"><button className={!yearly?"active":""} onClick={()=>setYearly(false)}>Monthly</button><button className={yearly?"active":""} onClick={()=>setYearly(true)}>Yearly <em>Save 25%</em></button></div><div className="pricing-grid">{plans.map(plan=><article className={plan.name==="Pro"?"price-card featured-price":"price-card"} key={plan.name}>{plan.name==="Pro"&&<span className="popular">MOST POPULAR</span>}<h2>{plan.name}</h2><p>{plan.name==="Free"?"For getting started":plan.name==="Pro"?"For creators ready to grow":"For teams and brands"}</p><strong>${yearly?plan.yearly:plan.monthly}<small>/month</small></strong><Link href="/register" className={plan.name==="Pro"?"btn primary":"btn secondary"}>{plan.name==="Free"?"Start free":"Choose "+plan.name}<ArrowRight size={16}/></Link><ul>{plan.features.map(f=><li key={f}><Check size={16}/>{f}</li>)}</ul></article>)}</div></main></>}
